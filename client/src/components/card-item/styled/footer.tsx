import styled from "@emotion/styled";

import { SPACE_IN_PX } from "src/common/constants/constants";

const Footer = styled.div`
  display: flex;
  margin-top: ${SPACE_IN_PX}px;
  align-items: center;
`;

export { Footer };
